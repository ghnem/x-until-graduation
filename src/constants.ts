const MILLISECONDS_TO_DAYS = 24 * 60 * 60 * 1000;

const firstDay = new Date('2017-09-24').getTime();
const lastDay = new Date('2022-08-01').getTime();

export { firstDay, lastDay, MILLISECONDS_TO_DAYS };
